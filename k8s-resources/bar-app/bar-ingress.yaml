apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bar-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: bar.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: bar-app-service
            port:
              number: 80